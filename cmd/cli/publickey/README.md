# Public key recovery CLI (moved)

This dev tool will live in `cafe-scanner-wallet` (see `cafe-discovery/TODO.md` PR4).

Until migration, use `cafe-scanner-wallet` wallet scan engine or `cmd/cli/wallet-scan` with an RPC URL and transaction hash.
